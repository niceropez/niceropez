import InicioPage from './InicioPage'

function SobreMiPage({ t, language, projectsTriggerVariant }) {
  return <InicioPage t={t} language={language} projectsTriggerVariant={projectsTriggerVariant} />
}

export default SobreMiPage
